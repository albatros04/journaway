import { destinations, showcaseTours } from "@/components/site-data";

export default function AdminDestinationsPage() { return <div className="admin-content"><section className="admin-panel"><div className="admin-panel-heading"><div><p className="eyebrow">Catalog</p><h2>Destinations</h2></div><p>Read-only view of the destinations presently published to JournAway.</p></div><div className="admin-destination-grid">{destinations.map(destination => <article key={destination.slug}><img src={destination.image} alt="" /><div><p>{destination.subtitle}</p><h3>{destination.name}</h3><span>{showcaseTours.filter(tour => tour.destination.includes(destination.name)).length} linked published tour{showcaseTours.filter(tour => tour.destination.includes(destination.name)).length === 1 ? "" : "s"}</span><a href={`/destinations/${destination.slug}`}>View public destination <b aria-hidden="true">→</b></a></div></article>)}</div></section><section className="admin-empty"><p className="eyebrow">Read-only catalog</p><h2>Destination changes require persistent source data.</h2><p>No destination database model or protected update API exists in the project today, so this screen intentionally contains no edit, activation or image-update control.</p></section></div>; }
