import { AdminEmptyState } from "@/components/admin-shell";
import { destinations, showcaseTours } from "@/components/site-data";

export default function AdminDashboard() {
  return <div className="admin-content"><section className="admin-metrics" aria-label="Published catalog overview"><article><span>Published tours</span><strong>{showcaseTours.length}</strong><a href="/admin/tours">View legacy catalog</a></article><article><span>Published destinations</span><strong>{destinations.length}</strong><a href="/admin/destinations">View catalog</a></article><article><span>Package editor</span><strong>+</strong><a href="/admin/packages">Manage packages</a></article></section><section className="admin-panel"><div className="admin-panel-heading"><div><p className="eyebrow">Available data</p><h2>Published catalog</h2></div><p>These figures are calculated from the same catalog currently shown on the public website.</p></div><div className="admin-catalog-list">{showcaseTours.map(tour => <a href={`/tours/${tour.slug}`} key={tour.slug}><img src={tour.image} alt="" /><div><strong>{tour.name}</strong><span>{tour.destination} · {tour.duration}</span></div><b aria-hidden="true">→</b></a>)}</div></section><AdminEmptyState title="Operational records are available through their protected APIs." copy="Use the package editor to manage new database-backed packages. Driver, hotel and booking records remain scoped to their protected operations APIs." /></div>;
}
